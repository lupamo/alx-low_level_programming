#include <stdio.h>
/**
 * main - basse numbers
 *
 * Return: 0 after successful executing
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		putchar('0' + i);
	}
	putchar('\n');

	return (0);
}
