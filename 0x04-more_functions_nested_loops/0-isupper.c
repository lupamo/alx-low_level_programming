#include "main.h"
/**
 * _isupper - checks if a character is upper
 * @c: input character
 * Return: 0 after succes
 */

int _isupper(int c)
{
	if (isupper(c)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
