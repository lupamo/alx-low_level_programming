#include "main.h"
/**
 * jack_bauer - prints every min
 *
 * Return: returns void
 */
void jack_bauer(void)
{
	for (int hour = 0; hour < 24; hour++)
		if (int minute = 0; minute < 60; minute++)
		{
			_putchar("%02d:%02d\n",hour, minute);
		}
	return (0);
}
